--- /tmp/pwrap.c.bak	2026-06-17 11:05:21.648502622 +0100
+++ drivers/soc/mediatek/mtk-pmic-wrap.c	2026-06-17 11:05:21.669911404 +0100
@@ -1327,6 +1327,7 @@
 	PWRAP_MT8195,
 	PWRAP_MT8365,
 	PWRAP_MT8516,
+	PWRAP_MT6582,
 };
 
 struct pmic_wrapper;
@@ -2277,6 +2278,22 @@
 	.init_soc_specific = pwrap_mt2701_init_soc_specific,
 };
 
+/* MT6582 (krillin): el LK ya inicializa el pwrap (INIT_DONE2=1) -> el driver salta
+ * pwrap_init. Sin reset-controller en el SoC -> caps=0 (sin PWRAP_CAP_RESET). regs
+ * = mt2701 (offsets WACS2 0x9c/a0/a4 coinciden con el HW, validado por poke). */
+static const struct pmic_wrapper_type pwrap_mt6582 = {
+	.regs = mt2701_regs,
+	.type = PWRAP_MT6582,
+	.arb_en_all = 0x3f,
+	.int_en_all = ~(u32)(BIT(31) | BIT(2)),
+	.int1_en_all = 0,
+	.spi_w = PWRAP_MAN_CMD_SPI_WRITE_NEW,
+	.wdt_src = PWRAP_WDT_SRC_MASK_ALL,
+	.caps = 0,
+	.init_reg_clock = pwrap_mt2701_init_reg_clock,
+	.init_soc_specific = NULL,
+};
+
 static const struct pmic_wrapper_type pwrap_mt6765 = {
 	.regs = mt6765_regs,
 	.type = PWRAP_MT6765,
@@ -2446,6 +2463,7 @@
 
 static const struct of_device_id of_pwrap_match_tbl[] = {
 	{ .compatible = "mediatek,mt2701-pwrap", .data = &pwrap_mt2701 },
+	{ .compatible = "mediatek,mt6582-pwrap", .data = &pwrap_mt6582 },
 	{ .compatible = "mediatek,mt6765-pwrap", .data = &pwrap_mt6765 },
 	{ .compatible = "mediatek,mt6779-pwrap", .data = &pwrap_mt6779 },
 	{ .compatible = "mediatek,mt6795-pwrap", .data = &pwrap_mt6795 },
