gl_kal_thread.c: Verificar en precomp.h/gl_typedef.h portado que GLUE_INFO_T declara: main_thread (struct task_struct*), prDevHandler (net_device*), waitq, u4Flag, rTxQueue, rCmdQueue, OidEntry (GL_IO_REQ_T), rPendComp, rHaltComp, rSpinLock[], i4TxPendingFrameNum, i4TxPendingSecurityFrameNum, u4OidCompleteFlag, u4OsMgmtFrameFilter, prAdapter. Todos existen en el stock gl_typedef.h.
gl_kal_thread.c: GLUE_FLAG_INT_BIT en el gl_os.h stock es 1 (no 2 como el fallback del hif.h del contrato); este .c usa el simbolo GLUE_FLAG_INT_BIT del header portado, asi que sera coherente con lo que ponga el ISR del HIF. Alinear el valor entre gl_os.h y el fallback de mt6582-hif.c en Fase 3.
gl_kal_thread.c: ktime_get_boot_ns() existe en 7.0.12; si el header portado ya define kalGetBootTime como macro, eliminar esta definicion para evitar redeclaracion.
gl_kal_thread.c: glStartTxThread/glStopTxThread no estan en el contrato gl_kal_prototypes.h; declararlos en gl_init.h (o al final de gl_kal_prototypes.h) en Fase 1 cuando se escriba gl_init.c, o gl_init dara warning de implicit-declaration.
gl_kal_thread.c: wlanReleasePendingOid(prAdapter, 0): confirmar la firma (adapter, u4Flag) en el core stock; coincide con el stock tx_thread.
gl_kal_thread.c: Confirmar que wlanIST (no wlanISR) es el punto de entrada del IST en el core dieted; el stock llama wlanIST(prAdapter) en la fase INT (wlanISR esta comentado).
gl_kal_ioctl.c: Verificar que GL_IO_REQ_T (P_GL_IO_REQ_T) expone los campos usados (prAdapter,pfnOidHandler,pvInfoBuf,u4InfoBufLen,pu4QryInfoLen,fgRead,fgWaitResp,rStatus,u4Flag,fgIsP2pOid) en el gl_typedef.h portado; fgIsP2pOid solo bajo CFG_ENABLE_WIFI_DIRECT.
gl_kal_ioctl.c: Confirmar que GLUE_INFO_T declara: OidEntry (GL_IO_REQ_T), rPendComp (struct completion), rPendStatus, u4OidCompleteFlag, ioctl_sem (struct semaphore), waitq, u4Flag, rCmdQueue, prAdapter. Varios los toca gl_init/gl_kal_thread de Fase 1.
gl_kal_ioctl.c: reinit_completion existe en 7.0.12 (reemplazo de INIT_COMPLETION 3.10); rPendComp debe inicializarse con init_completion() en gl_init (probe), no aqui.
gl_kal_ioctl.c: kalIsResetting no se declara en el contrato (CFG_CHIP_RESET_SUPPORT=0); el bloque queda compilado-fuera. Si en el futuro se activa reset, declararlo en gl_kal.h.
gl_kal_ioctl.c: wlanoidClearTimeoutCheck / cmdBufFreeCmdInfo / QUEUE_* / GLUE_SPIN_LOCK_* vienen del core (wlan_oid.h, cmm_asic_connac?, queue.h, gl_os.h) via precomp.h; validar la cadena de includes en Fase 3-4.
gl_kal_ioctl.c: DBGLOG con doble-parentesis por la macro variadica stock; si el gl_kal.h portado cambia DBGLOG a forma C99, ajustar.
gl_kal_timer.c: kalSetTimer aparece DECLARADO DOS VECES en gl_kal.h (forma OS_SYSTIME y forma UINT_32); en Fase 2 unificar la declaracion en gl_kal.h a (P_GLUE_INFO_T,UINT_32)->BOOLEAN para evitar warning de redeclaracion contra esta definicion.
gl_kal_timer.c: El parametro prTimerHandler de kalOsTimerInitialize se ignora (el trampolin fijo despierta el tx_thread, que corre cnmTimerDoTimeOutCheck). Confirmar en Fase 3 que el bucle del tx_thread (gl_kal_thread.c) trata GLUE_FLAG_TIMEOUT_BIT llamando a la logica del cnm_timer, ya que el core stock pasaba su handler por .function.
gl_kal_timer.c: kalGetTimeTick sigue siendo la macro de gl_kal.h; la definicion-funcion aqui esta bajo #ifndef y solo se activa si esa macro se retirara. Decidir en Fase 2 si se deja como macro (recomendado) y se elimina este bloque.
gl_kal_timer.c: timer_container_of/timer_setup/timer_delete_sync requieren <linux/timer.h> de 7.0.12; verificar que la version exacta del kernel ya renombro del_timer_sync->timer_delete_sync (si conviven ambos, ok; si solo existe el nuevo, este fichero ya es correcto).
gl_kal_indicate.c: Verificar en Fase 3-4 que WLAN_STATUS_UNSPECIFIED_FAILURE existe en el enum del core para el connect-fail; si el core usa otro codigo (p.ej. REASON_CODE_UNSPECIFIED como en el stock), ajustar. El stock pasaba REASON_CODE_UNSPECIFIED como status_code a cfg80211_connect_result, que es semanticamente un status IEEE, no un reason -> revisar cual espera wpa_supplicant.
gl_kal_indicate.c: cfg80211_connect_bss requiere el arg 'timeout' (NL80211_TIMEOUT_UNSPECIFIED); si el kernel 7.0.12 exportara la variante sin timeout, quitar el ultimo arg. Confirmar la firma exacta en net/cfg80211.h del arbol 7.0.12.
gl_kal_indicate.c: struct cfg80211_inform_bss.scan_width: en 7.0.12 podria haberse eliminado (algunos kernels recientes lo quitaron junto con NL80211_BSS_CHAN_WIDTH_20). Si no compila, borrar las asignaciones .scan_width en kalGetAisChannel/kalIndicateBssInfo/MEDIA_CONNECT.
gl_kal_indicate.c: El helper kalGetAisChannel asume 5GHz para canal>14; este port es 2.4GHz-only (MT6582) pero se deja generico. Verificar que NL80211_BAND_5GHZ esta declarado aunque la banda no se registre.
gl_kal_indicate.c: kalGetBootTime() debe existir (gl_kal_thread.c segun el contrato) para el timestamp del beacon; ya declarado en gl_kal_prototypes.h.
gl_kal_indicate.c: SPIN_LOCK_NET_DEV, GLUE_SPIN_LOCK_DECLARATION, GLUE_ACQUIRE/RELEASE_SPIN_LOCK deben resolver via precomp.h/gl_os.h. RCPI_TO_dBm, MAC2STR/MACSTR, DBGLOG, wlanQueryInformation, wlanoidQueryBssid/Ssid, wlanGetChannelNumberByNetwork, wlanGetTargetBssDescByNetwork, scanReportBss2Cfg80211, cnmTimerStopTimer, nicChannelNum2Freq, wlanReturnPacket, wlanCheckSystemConfiguration: todos del core stock, visibles por precomp.h.
gl_kal_fw.c: Confirmar en Fase 3 que glSetHifInfo (gl_init) hace prGlueInfo->rHifInfo.Dev = &pdev->dev ANTES del primer wlanAdapterStart (si no, kalFirmwareOpen devuelve FAILURE por Dev NULL).
gl_kal_fw.c: Colocar el blob stock WIFI_RAM_CODE en /lib/firmware/mediatek/mt6582/WIFI_RAM_CODE en el rootfs pmOS (o ajustar WIFI_FW_IMAGE_PATH).
gl_kal_fw.c: kalFirmwareOpen/Close/Load/Size son static aqui; si otro TU del port (p.ej. un OID de debug) los necesitara, promover a no-static y declarar en gl_kal_prototypes.h. El grep del core no muestra otros callers, asi que static es correcto en Fase 1.
gl_kal_fw.c: Verificar que DBGLOG/DEBUGFUNC/ASSERT/kalMemCopy llegan via precomp.h y que ALIGN_4 no colisiona (aqui tiene guard #ifndef); si config.h ya define ALIGN_4, el guard lo respeta.
gl_kal_fw.c: Si en Fase 2 se decide devolver prFw->data directamente (sin vmalloc) para ahorrar RAM, cambiar Mapping para no cerrar el handle y que Unmapping haga release_firmware (variante del bloque #if 0 stock).
gl_kal_mem.c: kalUpdateRxCSUMOffloadParam se referencia bajo CFG_TCP_IP_CHKSUM_OFFLOAD: en Fase 2 vive en este mismo shim o en gl_kal_dev; confirmar que esta declarada en gl_kal_prototypes.h (lo esta, bajo el mismo #if) antes de habilitar el offload.
gl_kal_mem.c: eth_hw_addr_set() requiere <linux/etherdevice.h> (ya incluido) y kernel >= 5.15; 7.0.12 lo tiene. Si en algun punto se compilara contra <5.15 caeria el build -> usar dev_addr_set(). Verificar la version exacta del arbol pmOS.
gl_kal_mem.c: kalIsResetting: el contrato lo excluye (dice que va bajo CFG_CHIP_RESET_SUPPORT=0 y que el core no lo llama en este build). Lo dejo con guard por seguridad; si el core NUNCA lo referencia, se puede borrar el bloque #if para no exportar simbolo muerto. Confirmar con grep en Fase 3.
gl_kal_mem.c: kalProcessRxPacket asume que pucPacketStart cae dentro del area lineal del skb reservado en kalPacketAlloc (head..end). Si el HIF entrega el RX en un buffer DMA separado (Fase 3 PDMA), habra que revisar (skb_reset_tail_pointer usa skb->head como base). Con el backend PIO actual el buffer ES el del skb, asi que es correcto.
gl_kal_mem.c: COPY_MAC_ADDR / GLUE_DEC_REF_CNT / NETWORK_TYPE_AIS_INDEX vienen de precomp.h (gl_os.h + nic). Confirmar que precomp.h del port los arrastra; si no, anadir el include nic/ que falte.