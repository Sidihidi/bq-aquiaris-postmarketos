diff --git a/bsd/arm/param.h b/bsd/arm/param.h
index a7f7204..293c2ef 100644
--- a/bsd/arm/param.h
+++ b/bsd/arm/param.h
@@ -87,6 +87,11 @@
 #define	M16KCLBYTES	(1 << M16KCLSHIFT)	/* size of a jumbo cluster */
 
 #define	MCLOFSET	(MCLBYTES - 1)
+/* fallback CONFIG_NMBCLUSTERS: la seleccion por etiquetas del MASTER no lo emite
+ * para maquinas nuevas; usamos el valor <bsmall> del MASTER */
+#undef CONFIG_NMBCLUSTERS
+#define CONFIG_NMBCLUSTERS      ((1024 * 256) / MCLBYTES)
+
 #ifndef NMBCLUSTERS
 #define	NMBCLUSTERS	CONFIG_NMBCLUSTERS	/* cl map size */
 #endif
diff --git a/libkern/c++/OSRuntime.cpp b/libkern/c++/OSRuntime.cpp
index d16fa34..9af0c4f 100644
--- a/libkern/c++/OSRuntime.cpp
+++ b/libkern/c++/OSRuntime.cpp
@@ -218,6 +218,8 @@ kern_os_malloc_size(void * addr)
 *********************************************************************/
 #if __GNUC__ >= 3
 void __cxa_pure_virtual( void )    { panic("%s", __FUNCTION__); }
+/* stub: en el kernel no hay descarga ordenada de destructores estaticos */
+int __cxa_atexit(void (*func)(void*), void *arg, void *dso) { (void)func; (void)arg; (void)dso; return 0; }
 #else
 void __pure_virtual( void )        { panic("%s", __FUNCTION__); }
 #endif
diff --git a/libkern/kxld/kxld_object.h b/libkern/kxld/kxld_object.h
index ab78f20..a6d8c88 100644
--- a/libkern/kxld/kxld_object.h
+++ b/libkern/kxld/kxld_object.h
@@ -56,7 +56,7 @@ kern_return_t kxld_object_init_from_macho(KXLDObject *object,
     u_char *file, u_long size, const char *name,
     struct kxld_array *section_order,
     cpu_type_t cputype, cpu_subtype_t cpusubtype, KXLDFlags flags)
-    __attribute__((nonnull(1,2,4) visibility("hidden")));
+    __attribute__((nonnull(1,2,4), visibility("hidden")));
 
 void kxld_object_clear(KXLDObject *object)
     __attribute__((nonnull, visibility("hidden")));
diff --git a/makedefs/MakeInc.def b/makedefs/MakeInc.def
index ee67e78..68f390e 100644
--- a/makedefs/MakeInc.def
+++ b/makedefs/MakeInc.def
@@ -121,7 +121,7 @@ endif
 export SUPPORTED_I386_MACHINE_CONFIGS := DEFAULT
 export SUPPORTED_X86_64_MACHINE_CONFIGS := DEFAULT
 export SUPPORTED_ARM_MACHINE_CONFIGS := S5L8920X S5L8922X S5L8930X ARMPBA8 SUN4I OMAP3530 OMAP335X RASPBERRYPI \
-					OMAP3430_RX51 ARM_RVEB_V6 S5L8720X S5L8900XRB MSM8960_TOUCHPAD IMX53
+					OMAP3430_RX51 ARM_RVEB_V6 S5L8720X S5L8900XRB MSM8960_TOUCHPAD IMX53 MT6582
 export SUPPORTED_ARM64_MACHINE_CONFIGS := DEFAULT VEXPRESS 
 
 #
@@ -321,6 +321,7 @@ ARCH_FLAGS_ARM_OMAP3430_RX51 = -arch armv7
 ARCH_FLAGS_ARM_OMAP335X	= -arch armv7
 ARCH_FLAGS_ARM_ARMPBA8	= -arch armv7
 ARCH_FLAGS_ARM_MSM8960_TOUCHPAD	= -arch armv7
+ARCH_FLAGS_ARM_MT6582	= -arch armv7
 ARCH_FLAGS_ARM_RASPBERRYPI	= -arch armv6
 ARCH_FLAGS_ARM_ARM_RVEB_V6	= -arch armv6
 ARCH_FLAGS_ARM_IMX53		= -arch armv7
diff --git a/osfmk/arm/asm_help.h b/osfmk/arm/asm_help.h
index 258b8fb..c0477c1 100644
--- a/osfmk/arm/asm_help.h
+++ b/osfmk/arm/asm_help.h
@@ -156,13 +156,13 @@
     mrc     p15, 0, register, c13, c0, 4
 
 #define IncrementPreemptLevel(register, scratch)                      \
-    ldr     scratch, [register, MACHINE_THREAD_PREEMPT_COUNT]     ;   \
+    ldr     scratch, [register, #MACHINE_THREAD_PREEMPT_COUNT]     ;   \
     adds    scratch, scratch, #1                                  ;   \
-    str     scratch, [register, MACHINE_THREAD_PREEMPT_COUNT]     ;
+    str     scratch, [register, #MACHINE_THREAD_PREEMPT_COUNT]     ;
 
 #define DecrementPreemptLevel(register, scratch)                      \
-    ldr     scratch, [register, MACHINE_THREAD_PREEMPT_COUNT]     ;   \
+    ldr     scratch, [register, #MACHINE_THREAD_PREEMPT_COUNT]     ;   \
     subs    scratch, scratch, #1                                  ;   \
-    str     scratch, [register, MACHINE_THREAD_PREEMPT_COUNT]     ;
+    str     scratch, [register, #MACHINE_THREAD_PREEMPT_COUNT]     ;
 
 #endif
diff --git a/osfmk/arm/bcopyinout.s b/osfmk/arm/bcopyinout.s
index ab024ef..75fa1f7 100644
--- a/osfmk/arm/bcopyinout.s
+++ b/osfmk/arm/bcopyinout.s
@@ -63,31 +63,31 @@ EnterARM(copyinmsg)
     stmfd   sp!,{r10,r11,lr}
     mov     r3, #0
     LoadThreadRegister(r10)
-    adr     r12, .Lcopyin_fault
-    str     r12, [r10, TH_RECOVER]
-    bl      .Lcopyin_guts
+    ldr     r12, =Lcopyin_fault
+    str     r12, [r10, #TH_RECOVER]
+    bl      Lcopyin_guts
     mov     r11, #0
-    str     r11, [r10, TH_RECOVER]
+    str     r11, [r10, #TH_RECOVER]
     mov     r0, #0
     ldmfd   sp!,{r10,r11,pc}
 
-.Lcopyin_fault:
+Lcopyin_fault:
     mov     r11, #0
-    str     r11, [r10, TH_RECOVER]
+    str     r11, [r10, #TH_RECOVER]
 	mov	r0, #0xE    /* EFAULT */
 	cmp	r3, #0x00
-	ldmgtfd	sp!, {r4-r7}		/* r3 > 0 Restore r4-r7 */
-	ldmltfd	sp!, {r4-r9}		/* r3 < 0 Restore r4-r9 */
+	ldmfdgt	sp!, {r4-r7}		/* r3 > 0 Restore r4-r7 */
+	ldmfdlt	sp!, {r4-r9}		/* r3 < 0 Restore r4-r9 */
 	ldmfd	sp!, {r10-r11, pc}
 
-.Lcopyin_guts:
+Lcopyin_guts:
 	pld	[r0]
 	/* Word-align the destination buffer */
 	ands	ip, r1, #0x03		/* Already word aligned? */
-	beq	.Lcopyin_wordaligned	/* Yup */
+	beq	Lcopyin_wordaligned	/* Yup */
 	rsb	ip, ip, #0x04
 	cmp	r2, ip			/* Enough bytes left to align it? */
-	blt	.Lcopyin_l4_2		/* Nope. Just copy bytewise */
+	blt	Lcopyin_l4_2		/* Nope. Just copy bytewise */
 	sub	r2, r2, ip
 	rsbs	ip, ip, #0x03
 	addne	pc, pc, ip, lsl #3
@@ -102,15 +102,15 @@ EnterARM(copyinmsg)
 	RETeq
 
 	/* Destination buffer is now word aligned */
-.Lcopyin_wordaligned:
+Lcopyin_wordaligned:
 	ands	ip, r0, #0x03		/* Is src also word-aligned? */
-	bne	.Lcopyin_bad_align	/* Nope. Things just got bad */
+	bne	Lcopyin_bad_align	/* Nope. Things just got bad */
 	cmp	r2, #0x08		/* Less than 8 bytes remaining? */
-	blt	.Lcopyin_w_less_than8
+	blt	Lcopyin_w_less_than8
 
 	/* Quad-align the destination buffer */
 	tst	r1, #0x07		/* Already quad aligned? */
-	ldrnet	ip, [r0], #0x04
+	ldrtne	ip, [r0], #0x04
 	strne	ip, [r1], #0x04
 	subne	r2, r2, #0x04
 	stmfd	sp!, {r4-r9}		/* Free up some registers */
@@ -118,10 +118,10 @@ EnterARM(copyinmsg)
 
 	/* Destination buffer quad aligned, source is word aligned */
 	subs	r2, r2, #0x80
-	blt	.Lcopyin_w_lessthan128
+	blt	Lcopyin_w_lessthan128
 
 	/* Copy 128 bytes at a time */
-.Lcopyin_w_loop128:
+Lcopyin_w_loop128:
 	ldrt	r4, [r0], #0x04		/* LD:00-03 */
 	ldrt	r5, [r0], #0x04		/* LD:04-07 */
 	pld	[r0, #0x18]		/* Prefetch 0x20 */
@@ -175,17 +175,17 @@ EnterARM(copyinmsg)
 	strd	r8, [r1], #0x08		/* ST:70-77 */
 	subs	r2, r2, #0x80
 	strd	r4, [r1], #0x08		/* ST:78-7f */
-	bge	.Lcopyin_w_loop128
+	bge	Lcopyin_w_loop128
 
-.Lcopyin_w_lessthan128:
+Lcopyin_w_lessthan128:
 	adds	r2, r2, #0x80		/* Adjust for extra sub */
-	ldmeqfd	sp!, {r4-r9}
+	ldmfdeq	sp!, {r4-r9}
 	RETeq
 	subs	r2, r2, #0x20
-	blt	.Lcopyin_w_lessthan32
+	blt	Lcopyin_w_lessthan32
 
 	/* Copy 32 bytes at a time */
-.Lcopyin_w_loop32:
+Lcopyin_w_loop32:
 	ldrt	r4, [r0], #0x04
 	ldrt	r5, [r0], #0x04
 	pld	[r0, #0x18]
@@ -200,11 +200,11 @@ EnterARM(copyinmsg)
 	strd	r8, [r1], #0x08
 	subs	r2, r2, #0x20
 	strd	r4, [r1], #0x08
-	bge	.Lcopyin_w_loop32
+	bge	Lcopyin_w_loop32
 
-.Lcopyin_w_lessthan32:
+Lcopyin_w_lessthan32:
 	adds	r2, r2, #0x20		/* Adjust for extra sub */
-	ldmeqfd	sp!, {r4-r9}
+	ldmfdeq	sp!, {r4-r9}
 	RETeq				/* Return now if done */
 
 	and	r4, r2, #0x18
@@ -236,19 +236,19 @@ EnterARM(copyinmsg)
 	RETeq				/* Return now if done */
 	mov	r3, #0x00
 
-.Lcopyin_w_less_than8:
+Lcopyin_w_less_than8:
 	subs	r2, r2, #0x04
-	ldrget	ip, [r0], #0x04
+	ldrtge	ip, [r0], #0x04
 	strge	ip, [r1], #0x04
 	RETeq				/* Return now if done */
 	addlt	r2, r2, #0x04
 	ldrbt	ip, [r0], #0x01
 	cmp	r2, #0x02
-	ldrgebt	r2, [r0], #0x01
+	ldrbtge	r2, [r0], #0x01
 	strb	ip, [r1], #0x01
-	ldrgtbt	ip, [r0]
-	strgeb	r2, [r1], #0x01
-	strgtb	ip, [r1]
+	ldrbtgt	ip, [r0]
+	strbge	r2, [r1], #0x01
+	strbgt	ip, [r1]
 	RET
 
 /*
@@ -256,17 +256,17 @@ EnterARM(copyinmsg)
  * The destination buffer (r1) is word aligned, but the source buffer
  * (r0) is not.
  */
-.Lcopyin_bad_align:
+Lcopyin_bad_align:
 	stmfd	sp!, {r4-r7}
 	mov	r3, #0x01
 	bic	r0, r0, #0x03
 	cmp	ip, #2
 	ldrt	ip, [r0], #0x04
-	bgt	.Lcopyin_bad3
-	beq	.Lcopyin_bad2
-	b	.Lcopyin_bad1
+	bgt	Lcopyin_bad3
+	beq	Lcopyin_bad2
+	b	Lcopyin_bad1
 
-.Lcopyin_bad1_loop16:
+Lcopyin_bad1_loop16:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #8
 #else
@@ -298,18 +298,18 @@ EnterARM(copyinmsg)
 	str	r5, [r1], #0x04
 	str	r6, [r1], #0x04
 	str	r7, [r1], #0x04
-.Lcopyin_bad1:
+Lcopyin_bad1:
 	subs	r2, r2, #0x10
-	bge	.Lcopyin_bad1_loop16
+	bge	Lcopyin_bad1_loop16
 
 	adds	r2, r2, #0x10
-	ldmeqfd	sp!, {r4-r7}
+	ldmfdeq	sp!, {r4-r7}
 	RETeq				/* Return now if done */
 	subs	r2, r2, #0x04
 	sublt	r0, r0, #0x03
-	blt	.Lcopyin_l4
+	blt	Lcopyin_l4
 
-.Lcopyin_bad1_loop4:
+Lcopyin_bad1_loop4:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #8
 #else
@@ -323,11 +323,11 @@ EnterARM(copyinmsg)
 	orr	r4, r4, ip, lsl #24
 #endif
 	str	r4, [r1], #0x04
-	bge	.Lcopyin_bad1_loop4
+	bge	Lcopyin_bad1_loop4
 	sub	r0, r0, #0x03
-	b	.Lcopyin_l4
+	b	Lcopyin_l4
 
-.Lcopyin_bad2_loop16:
+Lcopyin_bad2_loop16:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #16
 #else
@@ -359,18 +359,18 @@ EnterARM(copyinmsg)
 	str	r5, [r1], #0x04
 	str	r6, [r1], #0x04
 	str	r7, [r1], #0x04
-.Lcopyin_bad2:
+Lcopyin_bad2:
 	subs	r2, r2, #0x10
-	bge	.Lcopyin_bad2_loop16
+	bge	Lcopyin_bad2_loop16
 
 	adds	r2, r2, #0x10
-	ldmeqfd	sp!, {r4-r7}
+	ldmfdeq	sp!, {r4-r7}
 	RETeq				/* Return now if done */
 	subs	r2, r2, #0x04
 	sublt	r0, r0, #0x02
-	blt	.Lcopyin_l4
+	blt	Lcopyin_l4
 
-.Lcopyin_bad2_loop4:
+Lcopyin_bad2_loop4:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #16
 #else
@@ -384,11 +384,11 @@ EnterARM(copyinmsg)
 	orr	r4, r4, ip, lsl #16
 #endif
 	str	r4, [r1], #0x04
-	bge	.Lcopyin_bad2_loop4
+	bge	Lcopyin_bad2_loop4
 	sub	r0, r0, #0x02
-	b	.Lcopyin_l4
+	b	Lcopyin_l4
 
-.Lcopyin_bad3_loop16:
+Lcopyin_bad3_loop16:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #24
 #else
@@ -420,18 +420,18 @@ EnterARM(copyinmsg)
 	str	r5, [r1], #0x04
 	str	r6, [r1], #0x04
 	str	r7, [r1], #0x04
-.Lcopyin_bad3:
+Lcopyin_bad3:
 	subs	r2, r2, #0x10
-	bge	.Lcopyin_bad3_loop16
+	bge	Lcopyin_bad3_loop16
 
 	adds	r2, r2, #0x10
-	ldmeqfd	sp!, {r4-r7}
+	ldmfdeq	sp!, {r4-r7}
 	RETeq				/* Return now if done */
 	subs	r2, r2, #0x04
 	sublt	r0, r0, #0x01
-	blt	.Lcopyin_l4
+	blt	Lcopyin_l4
 
-.Lcopyin_bad3_loop4:
+Lcopyin_bad3_loop4:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #24
 #else
@@ -445,15 +445,15 @@ EnterARM(copyinmsg)
 	orr	r4, r4, ip, lsl #8
 #endif
 	str	r4, [r1], #0x04
-	bge	.Lcopyin_bad3_loop4
+	bge	Lcopyin_bad3_loop4
 	sub	r0, r0, #0x01
 
-.Lcopyin_l4:
+Lcopyin_l4:
 	ldmfd	sp!, {r4-r7}
 	mov	r3, #0x00
 	adds	r2, r2, #0x04
 	RETeq
-.Lcopyin_l4_2:
+Lcopyin_l4_2:
 	rsbs	r2, r2, #0x03
 	addne	pc, pc, r2, lsl #3
 	nop
@@ -482,32 +482,32 @@ EnterARM(copyoutmsg)
     stmfd sp!,{r10,r11,lr}
     LoadThreadRegister(r10)
     mov     r3, #0
-    adr     r12, .Lcopyout_fault
-    ldr     r11, [r10, TH_RECOVER]
-    str     r12, [r10, TH_RECOVER]
-    bl  .Lcopyout_guts
+    ldr     r12, =Lcopyout_fault
+    ldr     r11, [r10, #TH_RECOVER]
+    str     r12, [r10, #TH_RECOVER]
+    bl  Lcopyout_guts
     mov     r11, #0
-    str     r11, [r10, TH_RECOVER]
+    str     r11, [r10, #TH_RECOVER]
     mov r0, #0
     ldmfd   sp!,{r10,r11,pc}
 
-.Lcopyout_fault:
+Lcopyout_fault:
     mov     r11, #0
-    str     r11, [r10, TH_RECOVER]
+    str     r11, [r10, #TH_RECOVER]
 	mov r0, #0x0e
 	cmp	r3, #0x00
-	ldmgtfd	sp!, {r4-r7}		/* r3 > 0 Restore r4-r7 */
-	ldmltfd	sp!, {r4-r9}		/* r3 < 0 Restore r4-r9 */
+	ldmfdgt	sp!, {r4-r7}		/* r3 > 0 Restore r4-r7 */
+	ldmfdlt	sp!, {r4-r9}		/* r3 < 0 Restore r4-r9 */
 	ldmfd	sp!, {r10-r11, pc}
 
-.Lcopyout_guts:
+Lcopyout_guts:
 	pld	[r0]
 	/* Word-align the destination buffer */
 	ands	ip, r1, #0x03		/* Already word aligned? */
-	beq	.Lcopyout_wordaligned	/* Yup */
+	beq	Lcopyout_wordaligned	/* Yup */
 	rsb	ip, ip, #0x04
 	cmp	r2, ip			/* Enough bytes left to align it? */
-	blt	.Lcopyout_l4_2		/* Nope. Just copy bytewise */
+	blt	Lcopyout_l4_2		/* Nope. Just copy bytewise */
 	sub	r2, r2, ip
 	rsbs	ip, ip, #0x03
 	addne	pc, pc, ip, lsl #3
@@ -522,27 +522,27 @@ EnterARM(copyoutmsg)
 	RETeq
 
 	/* Destination buffer is now word aligned */
-.Lcopyout_wordaligned:
+Lcopyout_wordaligned:
 	ands	ip, r0, #0x03		/* Is src also word-aligned? */
-	bne	.Lcopyout_bad_align	/* Nope. Things just got bad */
+	bne	Lcopyout_bad_align	/* Nope. Things just got bad */
 	cmp	r2, #0x08		/* Less than 8 bytes remaining? */
-	blt	.Lcopyout_w_less_than8
+	blt	Lcopyout_w_less_than8
 
 	/* Quad-align the destination buffer */
 	tst	r0, #0x07		/* Already quad aligned? */
 	ldrne	ip, [r0], #0x04
 	subne	r2, r2, #0x04
-	strnet	ip, [r1], #0x04
+	strtne	ip, [r1], #0x04
 	
 	stmfd	sp!, {r4-r9}		/* Free up some registers */
 	mov	r3, #-1			/* Signal restore r4-r9 */
 
 	/* Destination buffer word aligned, source is quad aligned */
 	subs	r2, r2, #0x80
-	blt	.Lcopyout_w_lessthan128
+	blt	Lcopyout_w_lessthan128
 
 	/* Copy 128 bytes at a time */
-.Lcopyout_w_loop128:
+Lcopyout_w_loop128:
 	ldrd	r4, [r0], #0x08		/* LD:00-07 */
 	pld	[r0, #0x18]		/* Prefetch 0x20 */
 	ldrd	r6, [r0], #0x08		/* LD:08-0f */
@@ -596,17 +596,17 @@ EnterARM(copyoutmsg)
 	subs	r2, r2, #0x80
 	strt	r4, [r1], #0x04		/* ST:78-7b */
 	strt	r5, [r1], #0x04		/* ST:7c-7f */
-	bge	.Lcopyout_w_loop128
+	bge	Lcopyout_w_loop128
 
-.Lcopyout_w_lessthan128:
+Lcopyout_w_lessthan128:
 	adds	r2, r2, #0x80		/* Adjust for extra sub */
-	ldmeqfd	sp!, {r4-r9}
+	ldmfdeq	sp!, {r4-r9}
 	RETeq				/* Return now if done */
 	subs	r2, r2, #0x20
-	blt	.Lcopyout_w_lessthan32
+	blt	Lcopyout_w_lessthan32
 
 	/* Copy 32 bytes at a time */
-.Lcopyout_w_loop32:
+Lcopyout_w_loop32:
 	ldrd	r4, [r0], #0x08
 	pld	[r0, #0x18]
 	ldrd	r6, [r0], #0x08
@@ -621,11 +621,11 @@ EnterARM(copyoutmsg)
 	subs	r2, r2, #0x20
 	strt	r4, [r1], #0x04
 	strt	r5, [r1], #0x04
-	bge	.Lcopyout_w_loop32
+	bge	Lcopyout_w_loop32
 
-.Lcopyout_w_lessthan32:
+Lcopyout_w_lessthan32:
 	adds	r2, r2, #0x20		/* Adjust for extra sub */
-	ldmeqfd	sp!, {r4-r9}
+	ldmfdeq	sp!, {r4-r9}
 	RETeq				/* Return now if done */
 
 	and	r4, r2, #0x18
@@ -657,19 +657,19 @@ EnterARM(copyoutmsg)
 	RETeq				/* Return now if done */
 	mov	r3, #0x00
 
-.Lcopyout_w_less_than8:
+Lcopyout_w_less_than8:
 	subs	r2, r2, #0x04
 	ldrge	ip, [r0], #0x04
-	strget	ip, [r1], #0x04
+	strtge	ip, [r1], #0x04
 	RETeq				/* Return now if done */
 	addlt	r2, r2, #0x04
 	ldrb	ip, [r0], #0x01
 	cmp	r2, #0x02
-	ldrgeb	r2, [r0], #0x01
+	ldrbge	r2, [r0], #0x01
 	strbt	ip, [r1], #0x01
-	ldrgtb	ip, [r0]
-	strgebt	r2, [r1], #0x01
-	strgtbt	ip, [r1]
+	ldrbgt	ip, [r0]
+	strbtge	r2, [r1], #0x01
+	strbtgt	ip, [r1]
 	RET
 
 /*
@@ -677,17 +677,17 @@ EnterARM(copyoutmsg)
  * The destination buffer (r1) is word aligned, but the source buffer
  * (r0) is not.
  */
-.Lcopyout_bad_align:
+Lcopyout_bad_align:
 	stmfd	sp!, {r4-r7}
 	mov	r3, #0x01
 	bic	r0, r0, #0x03
 	cmp	ip, #2
 	ldr	ip, [r0], #0x04
-	bgt	.Lcopyout_bad3
-	beq	.Lcopyout_bad2
-	b	.Lcopyout_bad1
+	bgt	Lcopyout_bad3
+	beq	Lcopyout_bad2
+	b	Lcopyout_bad1
 
-.Lcopyout_bad1_loop16:
+Lcopyout_bad1_loop16:
 #ifdef	__ARMEB__
 	mov	r4, ip, lsl #8
 #else
@@ -719,18 +719,18 @@ EnterARM(copyoutmsg)
 	strt	r5, [r1], #0x04
 	strt	r6, [r1], #0x04
 	strt	r7, [r1], #0x04
-.Lcopyout_bad1:
+Lcopyout_bad1:
 	subs	r2, r2, #0x10
-	bge	.Lcopyout_bad1_loop16
+	bge	Lcopyout_bad1_loop16
 
 	adds	r2, r2, #0x10
-	ldmeqfd	sp!, {r4-r7}
+	ldmfdeq	sp!, {r4-r7}
 	RETeq				/* Return now if done */
 	subs	r2, r2, #0x04
 	sublt	r0, r0, #0x03
-	blt	.Lcopyout_l4
+	blt	Lcopyout_l4
 
-.Lcopyout_bad1_loop4:
+Lcopyout_bad1_loop4:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #8
 #else
@@ -744,11 +744,11 @@ EnterARM(copyoutmsg)
 	orr	r4, r4, ip, lsl #24
 #endif
 	strt	r4, [r1], #0x04
-	bge	.Lcopyout_bad1_loop4
+	bge	Lcopyout_bad1_loop4
 	sub	r0, r0, #0x03
-	b	.Lcopyout_l4
+	b	Lcopyout_l4
 
-.Lcopyout_bad2_loop16:
+Lcopyout_bad2_loop16:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #16
 #else
@@ -780,18 +780,18 @@ EnterARM(copyoutmsg)
 	strt	r5, [r1], #0x04
 	strt	r6, [r1], #0x04
 	strt	r7, [r1], #0x04
-.Lcopyout_bad2:
+Lcopyout_bad2:
 	subs	r2, r2, #0x10
-	bge	.Lcopyout_bad2_loop16
+	bge	Lcopyout_bad2_loop16
 
 	adds	r2, r2, #0x10
-	ldmeqfd	sp!, {r4-r7}
+	ldmfdeq	sp!, {r4-r7}
 	RETeq				/* Return now if done */
 	subs	r2, r2, #0x04
 	sublt	r0, r0, #0x02
-	blt	.Lcopyout_l4
+	blt	Lcopyout_l4
 
-.Lcopyout_bad2_loop4:
+Lcopyout_bad2_loop4:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #16
 #else
@@ -805,11 +805,11 @@ EnterARM(copyoutmsg)
 	orr	r4, r4, ip, lsl #16
 #endif
 	strt	r4, [r1], #0x04
-	bge	.Lcopyout_bad2_loop4
+	bge	Lcopyout_bad2_loop4
 	sub	r0, r0, #0x02
-	b	.Lcopyout_l4
+	b	Lcopyout_l4
 
-.Lcopyout_bad3_loop16:
+Lcopyout_bad3_loop16:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #24
 #else
@@ -841,18 +841,18 @@ EnterARM(copyoutmsg)
 	strt	r5, [r1], #0x04
 	strt	r6, [r1], #0x04
 	strt	r7, [r1], #0x04
-.Lcopyout_bad3:
+Lcopyout_bad3:
 	subs	r2, r2, #0x10
-	bge	.Lcopyout_bad3_loop16
+	bge	Lcopyout_bad3_loop16
 
 	adds	r2, r2, #0x10
-	ldmeqfd	sp!, {r4-r7}
+	ldmfdeq	sp!, {r4-r7}
 	RETeq				/* Return now if done */
 	subs	r2, r2, #0x04
 	sublt	r0, r0, #0x01
-	blt	.Lcopyout_l4
+	blt	Lcopyout_l4
 
-.Lcopyout_bad3_loop4:
+Lcopyout_bad3_loop4:
 #ifdef __ARMEB__
 	mov	r4, ip, lsl #24
 #else
@@ -866,15 +866,15 @@ EnterARM(copyoutmsg)
 	orr	r4, r4, ip, lsl #8
 #endif
 	strt	r4, [r1], #0x04
-	bge	.Lcopyout_bad3_loop4
+	bge	Lcopyout_bad3_loop4
 	sub	r0, r0, #0x01
 
-.Lcopyout_l4:
+Lcopyout_l4:
 	ldmfd	sp!, {r4-r7}
 	mov	r3, #0x00
 	adds	r2, r2, #0x04
 	RETeq
-.Lcopyout_l4_2:
+Lcopyout_l4_2:
 	rsbs	r2, r2, #0x03
 	addne	pc, pc, r2, lsl #3
 	nop
@@ -946,8 +946,8 @@ EnterARM(copyinstr)
 
    LoadThreadRegister(r4)
 
-	adr	r5, .Lcopystrfault
-	str	r5, [r4, TH_RECOVER]
+	ldr	r5, =Lcopystrfault
+	str	r5, [r4, #TH_RECOVER]
 
 1:	ldrbt	r5, [r0], #0x0001
 	add	r6, r6, #0x00000001
@@ -957,7 +957,7 @@ EnterARM(copyinstr)
 	bne	1b
 
 	mov	r0, #0x00000000
-	str	r0, [r4, TH_RECOVER]
+	str	r0, [r4, #TH_RECOVER]
 
 	teq	r5, #0x00000000
 	moveq	r0, #0x00000000
@@ -987,8 +987,8 @@ EnterARM(copyoutstr)
 
     LoadThreadRegister(r4)
 
-	adr	r5, .Lcopystrfault
-	str	r5, [r4, TH_RECOVER]
+	ldr	r5, =Lcopystrfault
+	str	r5, [r4, #TH_RECOVER]
 
 1:	ldrb	r5, [r0], #0x0001
 	add	r6, r6, #0x00000001
@@ -998,7 +998,7 @@ EnterARM(copyoutstr)
 	bne	1b
 
 	mov	r0, #0x00000000
-	str	r0, [r4, TH_RECOVER]
+	str	r0, [r4, #TH_RECOVER]
 
 	teq	r5, #0x00000000
 	moveq	r0, #0x00000000
@@ -1011,37 +1011,37 @@ EnterARM(copyoutstr)
 	bx      lr
 
 /* A fault occurred during the copy */
-.Lcopystrfault:
+Lcopystrfault:
 	mov	r1, #0x00000000
-	str	r1, [r4, TH_RECOVER]
+	str	r1, [r4, #TH_RECOVER]
 	RESTORE_REGS
     bx      lr
 
 copyio_kernel:
 	/* We're open to attack here since we don't really verify the addresses being copied... */
     cmp     r2, #0x10
-    blt     .Lcopyio_copy2
+    blt     Lcopyio_copy2
     orr     r3, r0, r1
     tst     r3, #3
-    bne     .Lcopyio_copy2
+    bne     Lcopyio_copy2
     sub     r2, r2, #8
-.Lcopyio_copy8_loop:
+Lcopyio_copy8_loop:
     ldr     r3, [r0], #4
     ldr     r12, [r0], #4
     str     r3, [r1], #4
     str     r12, [r1], #4
     subs    r2, r2, #8
-    bge     .Lcopyio_copy8_loop
+    bge     Lcopyio_copy8_loop
     adds    r2, r2, #8
-    beq 	.Lcopyio_exit
-.Lcopyio_copy2:
+    beq 	Lcopyio_exit
+Lcopyio_copy2:
     subs    r2, r2, #2
     ldrb    r3, [r0], #1
-    ldrplb  r12, [r0], #1
+    ldrbpl  r12, [r0], #1
     strb    r3, [r0], #1
-    strplb  r12, [r1], #1
-    bhi     .Lcopyio_copy2
-.Lcopyio_exit:
+    strbpl  r12, [r1], #1
+    bhi     Lcopyio_copy2
+Lcopyio_exit:
     movs    r0, #0
     bx      lr
 copyio_no_perms:
@@ -1056,10 +1056,10 @@ copyio_no_perms:
  */
 EnterARM(copyinframe)
 	stmfd	sp!,{r4}
-	adr 	r3, _copyinframe_error
+	ldr 	r3, =_copyinframe_error
 	LoadThreadRegister(r12)
-	ldr 	r4, [r12, TH_RECOVER]
-	str 	r3, [r12, TH_RECOVER]
+	ldr 	r4, [r12, #TH_RECOVER]
+	str 	r3, [r12, #TH_RECOVER]
 	ldmia	r0, {r2,r3}
 	stmia 	r1, {r2,r3}
 _copyinframe_success:
diff --git a/osfmk/arm/cswitch.s b/osfmk/arm/cswitch.s
index 444ce5b..7645e57 100644
--- a/osfmk/arm/cswitch.s
+++ b/osfmk/arm/cswitch.s
@@ -43,7 +43,7 @@
 EnterARM(Call_continuation)
     /* Set the new stack pointer. */
     LoadThreadRegister(r12)
-    ldr     sp, [r12, TH_PCB_ISS]
+    ldr     sp, [r12, #TH_PCB_ISS]
 
     /* Zero out frame pointer */
     mov     r7, #0
@@ -70,10 +70,10 @@ EnterARM(Call_continuation)
 EnterARM(Switch_context)
     /* Switch context to the new thread and save the continuation. */
     teq     r1, #0
-    strne   r1, [r0, TH_CONTINUATION]
+    strne   r1, [r0, #TH_CONTINUATION]
 
     /* Only store registers if there is a continuation */
-    ldreq   r3, [r0, TH_PCB_ISS]
+    ldreq   r3, [r0, #TH_PCB_ISS]
     addeq   r3, r3, #16
     stmiaeq r3!, {r4-lr}
 
@@ -84,11 +84,11 @@ EnterARM(Switch_context)
     /* Set old/new threads */
     mcr     p15, 0, r2, c13, c0, 4
 
-    ldr     r3, [r2, MACHINE_THREAD_CTHREAD_SELF]
+    ldr     r3, [r2, #MACHINE_THREAD_CTHREAD_SELF]
     mcr     p15, 0, r3, c13, c0, 3
 
     /* Load registers and go. */
-    ldr     r3, [r2, TH_PCB_ISS]
+    ldr     r3, [r2, #TH_PCB_ISS]
     add     r3, r3, #16
     ldmia   r3!, {r4-lr}
 
@@ -115,11 +115,11 @@ EnterARM(machine_load_context)
     mcr     p15, 0, r0, c13, c0, 4
 
     /* Set cthread value */
-    ldr     r1, [r0, MACHINE_THREAD_CTHREAD_SELF]
+    ldr     r1, [r0, #MACHINE_THREAD_CTHREAD_SELF]
     mrc     p15, 0, r2, c13, c0, 3
 
     /* Load registers and go. */
-    ldr     r3, [r0, TH_PCB_ISS]
+    ldr     r3, [r0, #TH_PCB_ISS]
     mov     r0, #0
 
     add     r3, r3, #16
@@ -132,7 +132,7 @@ EnterARM(machine_load_context)
 EnterARM(thread_syscall_return)
     cpsid   i
     LoadThreadRegister(r12)
-    ldr     r4, [r12, TH_PCB_USS]
+    ldr     r4, [r12, #TH_PCB_USS]
     str     r0, [r4]
     b       thread_return_join
 
@@ -150,8 +150,8 @@ thread_return_join:
     /* Check for pending ast */
     LoadThreadRegister(r12)
 
-    ldr     r8, [r12, MACHINE_THREAD_CPU_DATA]
-    ldr     r5, [r8, CPU_PENDING_AST]
+    ldr     r8, [r12, #MACHINE_THREAD_CPU_DATA]
+    ldr     r5, [r8, #CPU_PENDING_AST]
 
     cmp     r5, #0
     beq     return_to_user
@@ -167,7 +167,7 @@ thread_return_join:
 return_to_user:
     /* Restore registers */
     LoadThreadRegister(r12)
-    ldr     sp, [r12, TH_PCB_USS]
+    ldr     sp, [r12, #TH_PCB_USS]
     ldr     r0, [sp, #0x40]
 
     msr     spsr_cxsf, r0
diff --git a/osfmk/arm/hw_lock.s b/osfmk/arm/hw_lock.s
index 263769e..8a92fee 100644
--- a/osfmk/arm/hw_lock.s
+++ b/osfmk/arm/hw_lock.s
@@ -76,7 +76,7 @@ EnterARM(hw_lock_lock)
 hw_lock_lock_panic:
     mov     r1, r0
     ldr     r2, [r1]
-    adr     r0, _panicString
+    ldr     r0, =_panicString
     blx     _panic
     b       .
 _panicString:
@@ -268,7 +268,7 @@ rwldstore:
 rwldpanic:
     mov         r2, r1
     mov         r1, r0
-    adr         r0, lckPanicString
+    ldr         r0, =lckPanicString
     blx         _panic
 lckPanicString:
     .asciz "lck_rw_done(): lock (0x%08x: 0x%08x)"
@@ -301,7 +301,7 @@ EnterARM(lck_rw_lock_shared_to_exclusive)
 rwlsepanic:
     mov         r1, r2
     mov         r1, r0
-    adr         r0, lckReadToWritePanicString
+    ldr         r0, =lckReadToWritePanicString
     blx         _panic
 lckReadToWritePanicString:
     .asciz "lck_rw_lock_shared_to_exclusive(): lock (0x%08x 0x%08x)"
@@ -341,7 +341,7 @@ EnterARM(lock_write_to_read)
 rwlstexit:
     mov         r2, r1
     mov         r1, r0
-    adr         r0, rwlstePanicString
+    ldr         r0, =rwlstePanicString
     blx         _panic
 rwlstePanicString:
     .asciz "lck_rw_lock_exclusive_to_shared(): lock (0x%08x 0x%08x)"
@@ -402,7 +402,7 @@ lmuret:
 lmupanic:
     mov         r1, r0
     ldr         r2, [r1]
-    adr         r0, lmuPanicString
+    ldr         r0, =lmuPanicString
     blx         _panic
 lmuPanicString:
     .asciz      "lck_mtx_unlock(): mutex (0x%08x, 0x%08x)"
@@ -463,7 +463,7 @@ mlckwait:
 mlckpanic:
     mov     r1, r0
     ldr     r2, [r1]
-    adr     r0, mlckpanicString
+    ldr     r0, =mlckpanicString
     blx     _panic
 mlckpanicString:
     .asciz  "lck_mtx_lock(): mutex (0x%08x, 0x%08x)"
@@ -525,7 +525,7 @@ lmtret:
 lmtpanic:
     mov         r1, r0
     ldr         r2, [r1]
-    adr         r0, lmtPanicString
+    ldr         r0, =lmtPanicString
     blx         _panic
 lmtPanicString:
     .asciz "lck_mtx_try_lock(): mutex (0x%08x, 0x%08x)"
@@ -542,7 +542,7 @@ EnterARM(lck_mtx_assert)
     cmp     r3, r12
     bxeq    lr
     mov     r1, r0
-    adr     r0, panicString_lockNotOwned
+    ldr     r0, =panicString_lockNotOwned
     blx     _panic
 lck_mtx_assert_owned:
     cmp     r1, #2
@@ -551,10 +551,10 @@ lck_mtx_assert_owned:
     cmp     r3, r12
     bxne    lr
     mov     r1, r0
-    adr     r0, panicString_lockOwned
+    ldr     r0, =panicString_lockOwned
     blx     _panic
 lck_mtx_assert_bad_arg:
-    adr     r0, panicString_lockBadArgument
+    ldr     r0, =panicString_lockBadArgument
     blx     _panic
 
     .align 4
@@ -605,7 +605,7 @@ rwtlsfail:
 rwtlspanic:
     mov     r2, r1
     mov     r1, r0
-    adr     r0, rwtlsPanicString
+    ldr     r0, =rwtlsPanicString
     blx     _panic
 rwtlsPanicString:
     .asciz  "lck_rw_try_lock_shared: lock (0x%08x, 0x%08x)"
@@ -642,7 +642,7 @@ rwtlefail:
 rwtlepanic:
     mov     r2, r1
     mov     r1, r0
-    adr     r0, rwtlePanicString
+    ldr     r0, =rwtlePanicString
     blx     _panic
 rwtlePanicString:
     .asciz  "lck_rw_try_lock_exclusive: lock (0x%08x, 0x%08x)"
@@ -662,7 +662,7 @@ EnterARM(hw_lock_bit)
     bxeq    lr
 hwlbitpanic:
     mov     r0, r1
-    adr     r0, hwlbitPanicString
+    ldr     r0, =hwlbitPanicString
     blx     _panic
 hwlbitPanicString:
     .asciz "hw_lock_bit: lock 0x%08x"
diff --git a/osfmk/arm/locore.s b/osfmk/arm/locore.s
index 9f6a2ea..2f0ad4a 100644
--- a/osfmk/arm/locore.s
+++ b/osfmk/arm/locore.s
@@ -154,7 +154,7 @@ map:
       * xxx KASLR: we need to jump to a trampoline.
       * The address in r3 is relative, we convert it to a KVA and jump.
       */
-    adr     r3, start_trampoline
+    ldr     r3, =start_trampoline
     sub     r3, r3, r11
     add     r3, r3, r10
     bx      r3
@@ -278,7 +278,7 @@ EnterARM(sleep_test)
 #endif
 
     /* Jump to physical trampoline. */
-    adr     r4, sleep_tramp
+    ldr     r4, =sleep_tramp
     sub     r4, r4, r9
     add     r4, r4, r8
     bx      r4
diff --git a/osfmk/arm/machine_routines_asm.s b/osfmk/arm/machine_routines_asm.s
index 9604835..6c4c3e3 100644
--- a/osfmk/arm/machine_routines_asm.s
+++ b/osfmk/arm/machine_routines_asm.s
@@ -65,15 +65,15 @@ EnterARM(machine_idle)
 
     /* Disabled? We leave. */
     teq     r0, #0
-    beq     .Lmachine_idle_return
+    beq     Lmachine_idle_return
 
     /* Disable all interrupts and go into wfi mode. */
-.Lmachine_idle_wfi:
+Lmachine_idle_wfi:
     cpsid   if
     wfi
     cpsie   if
 
-.Lmachine_idle_return:
+Lmachine_idle_return:
     bx      lr
 
 /**
@@ -310,7 +310,7 @@ EnterARM(Halt_system)
     /* Disable interruptions. */
     cpsid    if
 
-.L_deadloop:
+L_deadloop:
     /* Drain write buffer. */
 #if __ARM_ARCH == 7
     dsb      sy
@@ -328,7 +328,7 @@ EnterARM(Halt_system)
 #endif
 
     /* Try again for a halt. */
-    b        .L_deadloop
+    b        L_deadloop
 
 LOAD_ADDR_GEN_DEF(do_power_save)
 LOAD_ADDR_GEN_DEF(CurrentThread)
diff --git a/osfmk/arm/param.h b/osfmk/arm/param.h
index 2d4c0ee..a93e457 100644
--- a/osfmk/arm/param.h
+++ b/osfmk/arm/param.h
@@ -87,6 +87,11 @@
 #define	M16KCLBYTES	(1 << M16KCLSHIFT)  /* size of a jumbo cluster */
 
 #define	MCLOFSET	(MCLBYTES - 1)
+/* fallback CONFIG_NMBCLUSTERS: la seleccion por etiquetas del MASTER no lo emite
+ * para maquinas nuevas; usamos el valor <bsmall> del MASTER */
+#undef CONFIG_NMBCLUSTERS
+#define CONFIG_NMBCLUSTERS      ((1024 * 256) / MCLBYTES)
+
 #ifndef NMBCLUSTERS
 #define	NMBCLUSTERS	CONFIG_NMBCLUSTERS  /* cl map size */
 #endif
diff --git a/osfmk/arm/traps_lo.s b/osfmk/arm/traps_lo.s
index d3f2d54..51dc47c 100644
--- a/osfmk/arm/traps_lo.s
+++ b/osfmk/arm/traps_lo.s
@@ -142,7 +142,7 @@ swi_trap_ret:
 
 swi_mach:
     /* Load the mach function from the mach trap table and call it. */
-    adr     lr, swi_exit
+    ldr     lr, =swi_exit
     mov     r4, r5
     cmp     r5, #0x80
     bge     swi_mach_error
@@ -577,7 +577,7 @@ restore_kernel_context:
 irqvec_panic:
     LOAD_ADDR(sp, irqstack_top)
     mov     r1, r0
-    adr     r0, vecPanicString
+    ldr     r0, =vecPanicString
     blx     _panic
     b       .
 
diff --git a/osfmk/conf/Makefile.template b/osfmk/conf/Makefile.template
index c39e844..07d2701 100644
--- a/osfmk/conf/Makefile.template
+++ b/osfmk/conf/Makefile.template
@@ -120,7 +120,7 @@ genassym.o: $(SOURCE_DIR)/$(COMPONENT)/$(GENASSYM_LOCATION)/genassym.c
 	$(_v)${KCC} $(subst -flto,,${CFLAGS}) -MD ${_HOST_EXTRA_CFLAGS} -S -o ${@} -c ${INCFLAGS} $<
 
 assym.s: genassym.o
-	$(_v)sed -e '/#DEFINITION#/!d' -e 's/^.*#DEFINITION#//' -e 's/\$$//' -e 'p' -e 's/#//2' -e 's/[^A-Za-z0-9_]*\([A-Za-z0-9_]*\)/ \1_NUM/2' genassym.o > ${@}
+	$(_v)python3 $(SRCROOT)/tools/gen_assym.py genassym.o > ${@}
 
 ${SOBJS}: assym.s
 
diff --git a/osfmk/conf/files.arm b/osfmk/conf/files.arm
index 7a82f36..17e5513 100644
--- a/osfmk/conf/files.arm
+++ b/osfmk/conf/files.arm
@@ -68,3 +68,4 @@ osfmk/kdp/ml/arm/kdp_arm_common.c      optional        mach_kdp
 osfmk/kdp/ml/arm/kdp_machdep.c         optional        mach_kdp
 osfmk/kdp/ml/arm/kdp_vm.c              optional        mach_kdp
 
+osfmk/arm/mem_aliases.s standard
diff --git a/osfmk/vm/vm_pageout.c b/osfmk/vm/vm_pageout.c
index 2bfd6e7..1a910bc 100644
--- a/osfmk/vm/vm_pageout.c
+++ b/osfmk/vm/vm_pageout.c
@@ -3022,7 +3022,7 @@ uint32_t vm_pageout_considered_page_last = 0;
  * called once per-second via "compute_averages"
  */
 void
-compute_pageout_gc_throttle()
+compute_pageout_gc_throttle(__unused void *arg)
 {
 	if (vm_pageout_considered_page != vm_pageout_considered_page_last) {
 
diff --git a/pexpert/conf/files.arm b/pexpert/conf/files.arm
index 6918d22..9ebd116 100644
--- a/pexpert/conf/files.arm
+++ b/pexpert/conf/files.arm
@@ -18,4 +18,5 @@ pexpert/arm/pe_s5l8720x.c		standard
 pexpert/arm/pe_s5l8900x.c		standard
 pexpert/arm/pe_s5l8930x.c		standard
 pexpert/arm/pe_apq8060.c		standard
+pexpert/arm/pe_mt6582.c		standard
 pexpert/arm/pe_imx53.c			standard
